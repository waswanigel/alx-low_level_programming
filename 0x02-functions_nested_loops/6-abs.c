#include "main.h"

/**
 * _abs - computes abs value of integer
 * @i: num whose abs value is computed
 *
 * Return: Abs value of integer
 */

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
		return (-i);
}
